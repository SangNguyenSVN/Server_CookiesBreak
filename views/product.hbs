<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product List</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }

    h1 {
      text-align: center;
      color: #333;
      margin-top: 20px;
    }

    .container {
      width: 80%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
      background: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      border-bottom: 1px solid #ddd;
      padding: 15px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    li:last-child {
      border-bottom: none;
    }

    h2 {
      margin: 0;
      font-size: 1.5em;
      color: #333;
    }

    p {
      margin: 5px 0;
      font-size: 1.2em;
      color: #555;
    }

    form {
      display: inline;
    }

    button {
      background-color: #007bff;
      border: none;
      color: white;
      padding: 10px 15px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 16px;
      margin: 5px;
      cursor: pointer;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #0056b3;
    }

    .link-container {
      text-align: center;
      margin-top: 20px;
    }

    a {
      text-decoration: none;
      color: #007bff;
      font-size: 1.2em;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Product List</h1>

    <!-- Check if there are any products -->
    {{#if products.length}}
      <ul>
        <!-- Loop through each product in the 'products' array -->
        {{#each products}}
          <li>
            <div>
              <h2>{{this.name}}</h2>
              <p>Price: ${{this.price}}</p>
            </div>
            <div>
              <!-- Update (Edit) action -->
              <form action="/product/update/{{this.id}}" method="GET" style="display:inline;">
                <button type="submit">Edit</button>
              </form>
              
              <!-- Delete action with confirmation -->
              <form action="/product/delete/{{this.id}}" method="POST" style="display:inline;" onsubmit="return confirmDeletion();">
                <button type="submit">Delete</button>
              </form>
            </div>
          </li>
        {{/each}}
      </ul>
    {{else}}
      <p>No products available.</p>
    {{/if}}

    <!-- Link to Add Product Form -->
    <div class="link-container">
      <a href="/product/add">Add New Product</a>
    </div>
  </div>

  <script>
    function confirmDeletion() {
      return confirm('Are you sure you want to delete this product?');
    }
  </script>
</body>
</html>
